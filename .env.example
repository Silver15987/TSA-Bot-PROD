# Discord Bot Configuration
DISCORD_BOT_TOKEN=your_discord_bot_token_here
DISCORD_CLIENT_ID=your_discord_client_id_here

# Azure Cosmos DB (MongoDB API)
# For Azure Cosmos DB, use format: mongodb://USERNAME:PASSWORD@HOST:PORT/?ssl=true&replicaSet=globaldb&retrywrites=false&maxIdleTimeMS=120000
COSMOS_DB_URI=mongodb://USERNAME:PASSWORD@HOST:PORT/?ssl=true&replicaSet=globaldb&retrywrites=false&maxIdleTimeMS=120000
COSMOS_DB_NAME=discord_bot

# Azure Redis Cache
REDIS_HOST=your-redis.redis.cache.windows.net
REDIS_PORT=6380
REDIS_USERNAME=default
REDIS_PASSWORD=your_redis_password_here
REDIS_TLS_ENABLED=true

# Environment
NODE_ENV=development

# Logging
LOG_LEVEL=info

# VC Tracking Configuration
VC_CATEGORY_ID=719217565263593482

# Webhook Server (for config hot-reload from dashboard)
WEBHOOK_SECRET=change_me_in_production
WEBHOOK_PORT=3001

# Bot Configuration (These values are used as defaults for new servers, actual values stored in database)
COMMAND_PREFIX=!
MAX_FACTIONS_PER_SERVER=50
FACTION_CREATE_COST=10000
DAILY_UPKEEP_COST=500

# Economy Configuration
COINS_PER_SECOND=0.1
MUTED_MULTIPLIER=0.5

# Gambling Configuration
COINFLIP_MIN_BET=100
COINFLIP_MAX_BET=50000
COINFLIP_HOUSE_EDGE=0.02
SLOTS_MIN_BET=50
SLOTS_MAX_BET=25000
SLOTS_HOUSE_EDGE=0.05
